export interface GenericBottomSheetItem {
  id: string;
  label: string;
  hex?: string;
}

export interface Category {
  id: string;
  label: string;
}

export interface Tag {
  id: string;
  label: string;
}
